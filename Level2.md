- 실수와 시행착오를 마주했던 경험

미션을 거듭하면서 도메인의 범위가 넓어지고, 서비스가 커지다 보니 생긴 것도 비슷하고 설계가 비슷한 도메인이 많이 만들어졌다. 예를 들면 일반 상품 `Product`와 카트에 담겨있는 상품 `CartProduct` 와 같은 객체들이 만들어졌다. `Product`와 `CartProduct` 모두 UUID 타입의 `id` 속성을 갖고 있었다. 

만약 추가하려는 상품과 같은 상품이 장바구니에 담겨있다면 해당 상품의 개수만 추가하는 방식으로 구현을 할 것이다. 그러려면 `cartProduct.product.id == product.id`와 같이 구현을 해야했다. 근데 헷갈려서 `cartProduct.id == product.id`와 같이 구현 한 것이다. 즉, 장바구니에 담긴 상품으로서 의미를 갖는 `CartProduct`의 `id`와 상품 그 자체의 의미를 갖는`Product`의 `id`를 비교했다. 따라서 해당 수식은 항상 false 일 수밖에 없는 수식이 되어버렸다. 

지금 돌이켜보면 원시값 포장을 사용하면 헷갈릴 일이 없었지 않았을까 싶다. 헷갈릴 수 있는 값들은 원시값 포장으로 묶어버리면 코드 작성 단계에서 두 id가 비교 불가능함을 할 수 있었을 것 같은데 좀 아쉬웠다. 

- 동료가 안전하게 배울 수 있도록 도왔던 경험

페어 프로그래밍을 하면서 일급 컬렉션을 왜 써야하는지에 대해 열띤 토론을 했다. 페어는 일급 컬렉션에 대한 개념이 없었는데, "이게 일급 컬렉션이다"라고 알려주면 페어가 그냥 수긍해버릴 것 같아서 일급 컬렉션이라고는 말 안하고 일급 컬렉션을 써야하는 이유를 들었다.

페어는 과한 추상화와 래핑이다, 그리고 왜 존재 해야 하는지, 책임이 모호하면서 응집되어있지 않다라고 말했다. 당시 구조가 `Product` ← `Products` ← `Cart`로 이어지는 위임의 연속이였는데, `Products`는 `Product`의 집합, 그리고 `Cart`는 카트에 담겨져 있는 `Products`를 의미했다. 하지만 `Cart`가 하는 일은 내부의 함수가 호출되면 `Products`내의 함수를 호출하는 역할만 담당하고 있었다.

과한 래핑이라는 말에 어느 정도 동의를 했다. 그래도 나는 `Cart`가 주는 도메인으로서의 상징성과 확장성을 들었다. 결국 페어를 설득하긴 했다. 거의 일급 컬렉션 내용만으로 1시간 반 정도를 토론 한 것 같은데, 그래도 페어가 포기하지 않고 끝까지 설득을 시도하려고 서로서로 했다는 점에서 소통의 재미를 느꼈다. 나도 과한 래핑이란 것을 인지하기도 했고, 반대로 페어에게도 일급 컬렉션의 개념과 장점을 이해시켰다는 점에서 서로 만족할만한 결과물을 내놓을 수 있었다고 생각한다.
